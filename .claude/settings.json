{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; if [[ \"$f\" == */SKILL.md ]]; then ./scripts/update-readme.sh; fi; } 2>/dev/null || true",
            "timeout": 10,
            "statusMessage": "Updating README skills table..."
          }
        ]
      }
    ]
  }
}
